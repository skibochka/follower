require('dotenv').config();

const constants = {
  botToken: process.env.BOT_TOKEN,
  username: process.env.USERNAME,
  password: process.env.PASSWORD,
  sendTo: 1555176503,
  nickname: 'nikto.ne.nyzhen',
};

export default constants;
